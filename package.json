{
  "name": "assetlink-with-qr-code",
  "version": "1.0.0",
  "workspaces": [
    "landing",
    "dashboard",
    "backend"
  ],
  "description": "AssetLink is a school asset management and QR-enabled repair tracking web app for Philippine public schools under DepEd. Teachers scan QR codes on classroom assets to report damage, which automatically generates repair tickets routed to principals, maintenance staff, barangay officials, and DepEd supervisors. The system replaces manual logbooks and paper forms with a transparent, accountable digital workflow — ensuring classrooms stay well-maintained and repair requests never get lost.",
  "main": "index.js",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:dashboard": "npm run dev --prefix dashboard",
    "dev:landing": "npm run dev --prefix landing",
    "dev": "concurrently --kill-others -n \"BACKEND,DASHBOARD,LANDING\" -c \"bgBlue.bold,bgCyan.bold,bgMagenta.bold\" \"npm run dev:backend\" \"npm run dev:dashboard\" \"npm run dev:landing\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix dashboard && npx pnpm --dir landing install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bagatata05/ASSETLINK-WITH-QR-CODE.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/bagatata05/ASSETLINK-WITH-QR-CODE/issues"
  },
  "homepage": "https://github.com/bagatata05/ASSETLINK-WITH-QR-CODE#readme",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^19",
      "@types/react-dom": "^19"
    }
  }
}
